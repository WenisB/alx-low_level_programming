#include <stdio.h>
/**
 * main - Entry point Print all single digit numbers for 0 to 9
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");

	return (0);
}
