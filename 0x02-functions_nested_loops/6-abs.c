#include "main.h"

/**
 * _abs - prints the absolut value of an input number
 *@x: value to inspect
 * Return: Always positive integer
 */
int _abs(int x)
{
	if (x > 0)
		return (x);
	else
		return (-x);
}
